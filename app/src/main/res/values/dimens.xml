<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>

    <!-- Default padding for content -->
    <dimen name="content_padding_normal">8dp</dimen>

    <!-- =========================== -->
    <!-- Dimensions related to lists -->
    <!-- =========================== -->
    <eat-comment />

    <!-- ======================================== -->
    <!-- Dimensions for one-line lists/list items -->
    <!-- ======================================== -->
    <eat-comment />

    <!-- The top and bottom padding of a one-line list (taken from the Material specs). -->
    <dimen name="one_line_list_padding_top_bottom">8dp</dimen>

    <!-- The height of a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_height">48dp</dimen>
    <!-- The left padding of a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_padding_left">16dp</dimen>

    <!-- The height of a one-line list item with a trailing icon (taken from the Material specs). -->
    <dimen name="one_line_list_item_height_with_icon">56dp</dimen>
    <!-- The height of the trailing icon in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_icon_height">24dp</dimen>
    <!-- The width of the trailing icon in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_icon_width">24dp</dimen>
    <!-- The top and bottom padding of the trailing icon in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_icon_padding_top_bottom">16dp</dimen>
    <!-- The left padding of text to the trailing icon in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_text_padding_left_icon">32dp</dimen>

    <!-- The height of a one-list item with a trailing avatar (taken from the Material specs). -->
    <dimen name="one_line_list_item_height_with_avatar">56dp</dimen>
    <!-- The height of the trailing avatar in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_avatar_height">40dp</dimen>
    <!-- The width of the trailing avatar in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_avatar_width">40dp</dimen>
    <!-- The left padding of text to the trailing avatar in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_text_padding_left_avatar">16dp</dimen>

    <!-- The height of a one-line list item with a trailing square image (taken from the Material specs). -->
    <dimen name="one_line_list_item_height_with_square_image">72dp</dimen>
    <!-- The height of the trailing square image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_square_image_height">56dp</dimen>
    <!-- The width of the trailing square image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_square_image_width">56dp</dimen>
    <!-- The left padding of text to the trailing square image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_text_padding_left_square_image">16dp</dimen>

    <!-- The height of a one-line list item with a trailing 100x56 image (taken from the Material specs). -->
    <dimen name="one_line_list_item_height_with_100x56_image">72dp</dimen>
    <!-- The height of the trailing 100x56 image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_100x56_image_height">56dp</dimen>
    <!-- The width of the trailing 100x56 image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_100x56_image_width">100dp</dimen>
    <!-- The left padding of text to the trailing 100x56 image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_text_padding_left_100x56_image">16dp</dimen>

    <!-- The height of the metadata image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_meta_image_height">24dp</dimen>
    <!-- The width of the metadata image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_meta_image_width">24dp</dimen>

    <!-- The left and right padding of the metadata image in a one-line list item (taken from the Material specs). -->
    <dimen name="one_line_list_item_meta_image_padding_left_right">16dp</dimen>
    <!-- The top and bottom padding of the metadata image in a one-line list item (taken from the Material specs).
    -->
    <dimen name="one_line_list_item_meta_image_padding_top_bottom">12dp</dimen>
    <!-- The top and bottom padding of the metadata image in a one-line list item with a trailing icon (taken from the
    Material specs). -->
    <dimen name="one_line_list_item_meta_image_padding_top_bottom_icon">16dp</dimen>
    <!-- The top and bottom padding of the metadata image in a one-line list item with a trailing avatar (taken from the
    Material specs). -->
    <dimen name="one_line_list_item_meta_image_padding_top_bottom_avatar">16dp</dimen>
    <!-- The top and bottom padding of the metadata image in a one-line list item with a trailing square image (taken from
    the Material specs). -->
    <dimen name="one_line_list_item_meta_image_padding_top_bottom_square_image">24dp</dimen>
    <!-- The top and bottom padding of the metadata image in a one-line list item with a 100x56 image (taken from the
    Material specs). -->
    <dimen name="one_line_list_item_meta_image_padding_top_bottom_100x56_image">24dp</dimen>

    <!-- ================================== -->
    <!-- Dimensions for two-line list items -->
    <!-- ================================== -->
    <eat-comment />

    <!-- The top and bottom padding of a two-line list (taken from the Material specs). -->
    <dimen name="two_line_list_padding_top_bottom">8dp</dimen>

    <!-- The height of a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_height">64dp</dimen>
    <!-- The left padding of a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_padding_left">16dp</dimen>

    <!-- The height of a two-line list item with a trailing icon (taken from the Material specs). -->
    <dimen name="two_line_list_item_height_with_icon">72dp</dimen>
    <!-- The height of the trailing icon in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_icon_height">24dp</dimen>
    <!-- The width of the trailing icon in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_icon_width">24dp</dimen>
    <!-- The top padding of the trailing icon in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_icon_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing icon in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_text_padding_left_icon">32dp</dimen>

    <!-- The height of a two-line list item with a trailing avatar (taken from the Material specs). -->
    <dimen name="two_line_list_item_height_with_avatar">72dp</dimen>
    <!-- The height of the trailing avatar in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_avatar_height">40dp</dimen>
    <!-- The width of the trailing avatar in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_avatar_width">40dp</dimen>
    <!-- The top padding of the trailing avatar in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_avatar_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing avatar in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_text_padding_left_avatar">16dp</dimen>

    <!-- The height of a two-line list item with a trailing square image (taken from the Material specs). -->
    <dimen name="two_line_list_item_height_with_square_image">72dp</dimen>
    <!-- The height of the trailing square image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_square_image_height">40dp</dimen>
    <!-- The width of the trailing square image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_square_image_width">40dp</dimen>
    <!-- The top padding of the trailing square image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_square_image_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing square image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_text_padding_left_square_image">16dp</dimen>

    <!-- The height of a two-line list item with a trailing 100x56 image (taken from the Material specs). -->
    <dimen name="two_line_list_item_height_with_100x56_image">72dp</dimen>
    <!-- The height of the trailing 100x56 image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_100x56_image_height">56dp</dimen>
    <!-- The width of the trailing 100x56 image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_100x56_image_width">100dp</dimen>
    <!-- The left padding of text to the trailing 100x56 image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_text_padding_left_100x56_image">16dp</dimen>

    <!-- The height of the metadata image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_image_height">24dp</dimen>
    <!-- The width of the metadata image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_image_width">24dp</dimen>
    <!-- The right padding of the metadata image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_image_padding_right">16dp</dimen>
    <!-- The left padding of the metadata image in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_image_padding_left">16dp</dimen>
    <!-- The left padding of the metadata text in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_text_padding_left">28dp</dimen>
    <!-- The top padding of the metadata in a two-line list item (taken from the Material specs). -->
    <dimen name="two_line_list_item_meta_padding_top">24dp</dimen>

    <!-- ====================================== -->
    <!-- Text baselines for two-line list items -->
    <!-- ====================================== -->
    <eat-comment />

    <!-- Baseline for the primary text of a normal variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_normal_list_item_primary_text_baseline">28dp</dimen>
    <!-- Baseline for the secondary text of a normal variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_normal_list_item_secondary_text_baseline">20dp</dimen>

    <!-- Baseline for the overline text of a overline variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_overline_list_item_overline_text_baseline">24dp</dimen>
    <!-- Baseline for the primary text of a overline variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_overline_list_item_primary_text_baseline">20dp</dimen>

    <!-- Baseline for the primary text of a trailing icon variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_icon_list_item_primary_text_baseline">32dp</dimen>
    <!-- Baseline for the secondary text of a trailing icon variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_icon_list_item_secondary_text_baseline">20dp</dimen>

    <!-- Baseline for the primary text of a trailing avatar variant two-line list item (taken from the Material specs). -->
    <dimen name="two_line_avatar_list_item_primary_text_baseline">32dp</dimen>
    <!-- Baseline for the secondary text of a trailing avatar variant two-line list item (taken from the Material specs).
    -->
    <dimen name="two_line_avatar_list_item_secondary_text_baseline">20dp</dimen>

    <!-- Baseline for the primary text of a trailing square image variant two-line list item (taken from the Material
    specs). -->
    <dimen name="two_line_square_image_list_item_primary_text_baseline">32dp</dimen>
    <!-- Baseline for the secondary text of a trailing square image variant two-line list item (taken from the Material
    specs). -->
    <dimen name="two_line_square_image_list_item_secondary_text_baseline">20dp</dimen>

    <!-- Baseline for the primary text of a trailing 100x56 image variant two-line list item (taken from the Material
    specs). -->
    <dimen name="two_line_100x56_image_list_item_primary_text_baseline">32dp</dimen>
    <!-- Baseline for the secondary text of a trailing 100x56 image variant two-line list item (taken from the Material
    specs). -->
    <dimen name="two_line_100x56_image_list_item_secondary_text_baseline">20dp</dimen>

    <!-- ==================================== -->
    <!-- Dimensions for three-line list items -->
    <!-- ==================================== -->
    <eat-comment />

    <!-- The top and bottom padding of a three-line list (taken from the Material specs). -->
    <dimen name="three_line_list_padding_top_bottom">8dp</dimen>

    <!-- The height of a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_height">88dp</dimen>
    <!-- The left padding of a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_padding_left">16dp</dimen>

    <!-- The height of the trailing icon in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_icon_height">24dp</dimen>
    <!-- The with of the trailing icon in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_icon_width">24dp</dimen>
    <!-- The top padding of the trailing icon in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_icon_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing icon in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_text_padding_left_icon">32dp</dimen>

    <!-- The height of the trailing avatar in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_avatar_height">40dp</dimen>
    <!-- The width of the trailing avatar in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_avatar_width">40dp</dimen>
    <!-- The top padding of the trailing avatar in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_avatar_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing avatar in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_text_padding_left_avatar">16dp</dimen>

    <!-- The height of the trailing square image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_square_image_height">40dp</dimen>
    <!-- The width of the trailing square image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_square_image_width">40dp</dimen>
    <!-- The top padding of the trailing square image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_square_image_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing square image in a three-line list item (taken from the Material specs).
    -->
    <dimen name="three_line_list_item_text_padding_left_square_image">16dp</dimen>

    <!-- The height of the trailing 100x56 image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_100x56_image_height">56dp</dimen>
    <!-- The width of the trailing 100x56 image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_100x56_image_width">100dp</dimen>
    <!-- The top padding of the trailing 100x56 image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_100x56_image_padding_top">16dp</dimen>
    <!-- The left padding of text to the trailing 100x56 image in a three-line list item (taken from the Material specs).
    -->
    <dimen name="three_line_list_item_text_padding_left_100x56_image">20dp</dimen>

    <!-- The right padding of the metadata in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_meta_padding_right">16dp</dimen>
    <!-- The left padding of the metadata image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_meta_icon_padding_left">16dp</dimen>
    <!-- The top padding of the metadata image in a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_meta_image_padding_top">16dp</dimen>

    <!-- Baseline for the primary text of a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_primary_text_baseline">28dp</dimen>
    <!-- Baseline for the secondary text of a three-line list item (taken from the Material specs). -->
    <dimen name="three_line_list_item_secondary_text_baseline">20dp</dimen>

    <!-- TODO: Add dimensions for collapsible/expandable list -->
    <!-- ================================== -->
    <!-- Dimensions for modal bottom sheets -->
    <!-- ================================== -->
    <eat-comment />

    <!-- The height of a list item in a modal bottom sheet (taken from the Material specs). -->
    <dimen name="modal_bottom_sheet_list_item_height">56dp</dimen>
    <!-- The left padding of a list item to its contents in a modal bottom sheet (taken from the Material specs). -->
    <dimen name="modal_bottom_sheet_list_item_padding_left">16dp</dimen>
    <!-- The left padding of text to the icon in a list item in a modal bottom sheet (taken from the Material specs). -->
    <dimen name="modal_bottom_sheet_list_item_text_padding_left_icon">32dp</dimen>
    <!-- The icon's height in a list item in a modal bottom sheet (taken from the Material specs). -->
    <dimen name="modal_bottom_sheet_list_item_icon_height">24dp</dimen>
    <!-- The icon's width in a list item in a modal bottom sheet (taken from the Material specs). -->
    <dimen name="modal_bottom_sheet_list_item_icon_width">24dp</dimen>
</resources>